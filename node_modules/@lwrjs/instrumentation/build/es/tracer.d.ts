import { LWRSpan } from './spans.js';
export declare enum TraceLevel {
    OFF = "off",
    DEFAULT = "default",
    VERBOSE = "verbose"
}
export interface LwrSpan {
    setAttributes(attributes: Record<string, string | number | boolean>): LwrSpan;
    end(): void;
}
interface TraceID {
    name: LWRSpan;
    attributes?: Record<string, string | number | boolean>;
}
/**
 *  Tracer - encapsulates open-telemetry tracing apis
 */
declare class Tracer {
    private traceLevel;
    startSpan(id: TraceID): LwrSpan;
    trace<T>(id: TraceID, fn: () => T): T;
}
/**
 * Tracer Singleton
 */
export declare const getTracer: () => Tracer;
export {};
//# sourceMappingURL=tracer.d.ts.map
import { SchematizedData } from 'o11y/shared';
import { LogMeta, Schema } from '../interfaces';
export type SenderLogMessage = {
    schema: Schema;
    data: SchematizedData;
    logMeta: LogMeta;
};

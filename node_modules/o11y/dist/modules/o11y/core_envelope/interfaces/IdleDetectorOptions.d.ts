import { ErrorListener, ReportListener } from './IdleDetector';
export interface IdleDetectorOptions {
    logThreshold?: number;
    reportListener?: ReportListener;
    errorListener?: ErrorListener;
}

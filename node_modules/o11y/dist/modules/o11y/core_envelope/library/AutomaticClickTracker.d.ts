import { Instrumentation } from '../interfaces';
export declare class AutomaticClickTracker {
    private readonly _instr;
    private readonly _doc;
    private _ignoredEvent;
    private _isActive;
    private readonly _boundClickListener;
    constructor(_instr: Instrumentation, _doc: Document);
    activate(): void;
    deactivate(): void;
    markEventHandled(event: MouseEvent): void;
    private _clickListener;
    private _getClickableElement;
}

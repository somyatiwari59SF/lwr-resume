import { SchematizedData } from 'o11y/shared';
import { LogCollector, LogMeta, MetricsCollector, MetricsExtractorMethods, Schema } from '../interfaces';
import { SenderConnector } from '../library/SenderConnector';
export declare const DEFAULT_POLL_INTERVAL = 1000;
export declare class SenderCollector implements LogCollector, MetricsCollector {
    private readonly _connector;
    private readonly _pollInterval;
    private _metricsExtractors;
    constructor(_connector: SenderConnector, _pollInterval?: number);
    collect(schema: Schema, data: SchematizedData, logMeta: LogMeta): void;
    receiveMetricsExtractors(extractors: MetricsExtractorMethods): void;
    private _collectAndSendMetrics;
}
